# InboxOS Desktop

`apps/desktop` is the planned macOS desktop shell for the shared InboxOS app.

Current status:

- desktop shell scaffold only
- intended to host the macOS runtime around the shared packages in `packages/`
- `packages/app`, `packages/features`, and `packages/ui` remain the shared UI source of truth
- `apps/web` is still the active production surface today
